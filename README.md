# Token-Based Authentication
User-facing client for the app.

> **NOTE**: This project has two parts, the backend `API` and the user-facing `Client`. The `api` and `client` branches contain the code for each of them, respectively.

```
The client is now complete. The only potential concern left now is that for as long as a valid access token (not expired) is available in the cookies, the user can access the application fully.
```
